# 宿主运行时与脚本 VM 集成

一旦引入脚本层，真正的难点不在脚本文件本身，而在宿主如何和脚本 VM 协作。

必须回答的问题包括：

1. 谁创建脚本环境
2. 生命周期如何管理
3. 宿主暴露哪些 API 给脚本
4. 脚本错误如何隔离
5. 脚本状态如何和宿主状态对齐

集成做得不好的常见后果是：

- 脚本能随意碰宿主内部状态
- 出错时整个运行时一起崩
- 调试时看不清宿主和脚本边界
- 性能问题无法定位

所以脚本 VM 最重要的不是“能跑起来”，而是边界清楚、错误可控、资源可回收。
