# Redis、排行榜与锁

Redis 在游戏项目里非常常见，但常见不代表应该无边界使用。

它通常适合：

- 高频缓存
- 短期状态
- 排行榜
- 计数器
- 限流和轻量协调

它不适合直接承担：

- 所有核心业务权威状态
- 复杂关系查询
- 无约束的大对象长期存储

排行榜是 Redis 很典型的强项，因为它天然需要：

- 按分值排序
- 快速取 TopN
- 局部区间查询

但排行榜真正的难点往往不在“能不能排”，而在：

- 分数如何更新
- 是否允许并发重算
- 排行结果和奖励结算如何对齐

锁也是一样。分布式锁不是不能用，而是要非常谨慎。很多游戏逻辑如果已经有天然权威单元，例如房间、战斗实例、角色主线程，其实不应该再用分布式锁硬兜。

锁能解决的是资源争用问题，不能替代状态边界设计。
